class Product {
  constructor(code, name, price, discount, offer) {
    this.code = code;
    this.name = name
    this.price = price
    this.discount = discount
    this.offer = offer
  }
}

module.exports = Product;
